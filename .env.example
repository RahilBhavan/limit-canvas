# Deploy (packages/contracts) — never commit real keys
DEPLOYER_KEY=
RPC_URL=http://127.0.0.1:8545
ETHERSCAN_API_KEY=

# Studio
NEXT_PUBLIC_DEFAULT_CHAIN_ID=1
# Optional: enable LLM assist (falls back to rules if unset)
OPENAI_API_KEY=
